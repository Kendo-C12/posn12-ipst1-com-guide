[<- หน้าก่อนหน้า](README.md) | [หน้าต่อไป ->](3_toi_zero.md)
---

# 2. แนะนำ grader

grader คือโปรแกรมที่เช็คว่าโค้ดเราถูกไหมโดยการลองใส่ Input แล้วเช็คว่า Output เหมือนกับค่าที่เขาคำนวณไว้ไหม

ทั้งนี้แนะนำว่าเวลาทำโจทย์ควรเช็คว่าโจทย์นั้นมีใน grader ตัวไหนก็ได้เพราะ โค้ดบ้างชุดแม้ว่า test case ที่เขาให้มาจะถูกแต่พอเลขใหญ่ขึ้นเรื่อยๆ โค้ดของเราอาจจะทำงานผิดพลาด

## 2.1 เนื้อหาที่ควรรู้

BigO notation คือ สัญกรณ์ทางคณิตศาสตร์ที่ใช้ในวิทยาการคอมพิวเตอร์ เพื่อประเมิน ประสิทธิภาพ ของ Algorithm โดยจะวัดในกรณี worst case

## 2.2 Time limit / การประมาณ maximum runtime (เวลามากสุดที่ใช้ในการทำงานของโปรแกรมเราในกรณี worst case)

Time limit คือการที่โค้ดของเราใช้เวลาในการทำงานเกินกว่าที่โจทย์กำหนด โดยส่วนใหญ่โจทย์จะ limit ที่ 1 วินาที

1 วินาที คอมพิวเตอร์เราจะทำการคำนวณได้ประมาณ 1e9 ครั้งแต่จะไม่เกิน 1e10 ครั้ง
trick ในการคำนวณว่าโปรแกรมเราจะผ่านไหมให้เราคำนวณ operation ที่คอมจะทำ โดยให้เราเอา BigO ที่เราคำนวณได้มาใส่ Maximum constrant เช่น

ให้ n <= 2e5 ถ้าโปรแกรมเราทำงานใน O(n²) จะได้ว่า maximum operation โดยประมาณคือ 4e10 ซึ่งเกิน 1e9

แต่ถ้าเราควรเขียนโปรแกรมที่ทำงานใน O(n log n) เราจะได้ maximum operation ที่ 4e6 ซึ่งน้อยกว่า 1e9 มาก

## 2.3 Memory

การจำกัดหน่วยความจำ (Memory Limit): โดยทั่วไปแล้ว ถ้าโจทย์ไม่ได้ตั้งใจจะทดสอบเราเรื่องการบริหาร Memory เป็นพิเศษ

เรามักจะไม่ค่อยเจอปัญหาส่งโค้ดแล้วติด Memory Limit เกินกำหนด

การคำนวณขนาดอาร์เรย์: หากโจทย์ให้ Memory Limit มา 256 MB จะสามารถประกาศตัวแปรชนิดอาร์เรย์จำนวนเต็ม int ได้สูงสุดประมาณ 6e7 ตัว

## 2.4 Runtime error

ลักษณะของ Runtime error คือการที่โปรแกรม run แล้วจะค้างสักพักนึงแล้วจะจบการทำงานโปรแกรมทันที

โดยเราสามารถให้โปรแกรมเรา print ค่าก่อนโดน runtime error โดยใช้คำสั่ง cerr แทนคำสั่ง cout

ในการสอบแล้วหากโปรแกรมเรา Runtime error ตัวของ Grader จะไม่ได้บอกว่าโปรแกรมเรา runtime error เลยแต่จะอยู่ในรูปแบบ

`Execution killed (could be triggered by violating memory limits)` ซึ่งในที่นี้มันจะเป็น memory limit หรือ runtime error ก็ได้

และเวลา run ในคอมเราปกติ คอมของเราแม้เราจะทำการ Runtime error แต่ในบ้างกรณีโปรแกรมก็ยังทำงานได้ปกติอยู่ดีเพราะฉะนั้นให้ระวังให้ดี

สาเหตุของ runtime error มักมาจาก

- การเข้าถึง Index array ที่มากกว่าที่ประกาศไว้หรือตำแหน่งติดลบก็ได้
- การประกาศ array ใหญ่เกินไป (memory limit)
- Infinity recursive loop (ในบ้างกรณีอาจจะค้างใน loop ไม่โดน runtime error ก็ได้)

ทั้งนี้การที่ตัวแปร overflow นั้นไม่นับเป็น runtime error เพราะงั้นถ้าโปรแกรมเราผิดในกรณีที่มีตัวเลขเยอะๆให้คิดไว้ก่อนว่าตัวแปร overflow

ซึ่งเราสามารถแก้ได้โดยการใช้ `long long`

## 2.5 Subtask

อธิบายง่ายๆคือเงื่อนไขในการให้คะแนน เช่นถ้าโปรแกรมเราใช้ได้ในกรณี n <= 8e3 จะได้ 50 คะแนน และถ้า n <= 2e5 จะได้อีก 50 คะแนน

ในบ้างครั้งถ้าน้องๆคิด solution ที่ได้ 100 คะแนนไม่ได้ พี่แนะนำให้น้องเก็บ subtask แต่น้องๆต้องมั่นใจว่าทำ task หลักไม่ได้จริงๆถึงค่อยเก็บ subtask เพราะบ้างข้อ การเก็บ subtask ก็ใช้เวลานานเหมือนกันโดยเฉพาะการเขียนแบบลองทุกเคส O(2^n) ซึ่งส่วนมากจะใช้เวลาเขียนนานกว่า solution ที่ได้ 100 คะแนน

---
[<- หน้าก่อนหน้า](README.md) | [หน้าต่อไป ->](3_toi_zero.md)
